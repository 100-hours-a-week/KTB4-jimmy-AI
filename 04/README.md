1. 가상 데이터셋을 생성한 뒤, 학습·검증·테스트 데이터셋으로 분할해 보세요.
2. 가상 데이터셋을 생성하고, K-최근접이웃(K-NN) 알고리즘으로 학습·예측을 수행해 보세요.
3. 동일한 이진 분류 가상 데이터셋을 생성하고, Perceptron, SVM, Random Forest, Naive Bayes 네 가지 알고리즘으로 학습해 보세요.
4.  가상 데이터셋을 준비하고, 증강(Data Augmentation) 기법을 적용했을 때와 적용하지 않았을 때 모델 성능을 비교하세요.
5. 활성화 함수를 직접 정의하고, 활성화 함수를 적용한 출력을 계산하고, 결과를 그래프로 시각화하세요.
6. 비선형 데이터셋을 생성하고, MLP(다층 퍼셉트론) 모델을 설계하고 학습시켜 분류를 수행하세요.
7. CNN(Convolutional Neural Network)을 직접 구성하여 이미지 분류를 수행하세요.

---
# 회고
개념공부에 집중하느랴 어떻게 '하느냐'는 사실 늦게 공부를 시작한 것 같다.

과제 1~3은 sklearn의 fit/predict 패턴만 알면 됐다. 모델을 만들고, 학습시키고, 예측하는 구조가 모든 알고리즘에서 동일해서 생각보다 빠르게 끝났다. 중간에 VSCode가 가상환경 대신 시스템 Python을 바라보고 있어서 sklearn을 못 찾는 문제가 있었는데, 인터프리터 설정을 바꿔서 해결했다.

과제 4 데이터 증강은 노이즈를 추가해 학습 데이터를 2배로 늘렸는데 결과가 거의 똑같았다. 가상으로 만든 깔끔한 데이터라그런지, 데이터 자체가 많지 않아서 그런지 노이즈를 줘도 모델이 배울 게 달라지지 않아서 그랬던 것 같다.

과제 5 활성화 함수 시각화는 plt.show()를 빠뜨려서 처음에 아무것도 안 나왔다. 세 함수를 한 그래프에 겹쳐서 보니 이론으로만 알던 모양이 직관적으로 들어왔다.

과제 6~7이 가장 재밌었다. 비선형 데이터를 ai에게 추천받아서 make_moons를 썼는데, 반달 모양이라 복잡해 보였지만 다층 퍼셉트론이 층마다 공간을 구부려가며 분리한다는 걸 알고 나니 납득이 됐다. XOR에서 배운 원리가 그대로 적용되는 것이었다. CNN은 5 epoch만에 99%가 나왔는데, MLP 88%와 나란히 놓고 보니 이미지에서 CNN이 왜 쓰이는지 숫자로 직접 확인되는 느낌이었다.

개념을 재료로 퍼즐을 푸는 것 같아서 재밌었다. 어떤 layer를 어떻게 조합할지 알려면 결국 각 부품을 제대로 알아야 한다는 것도 느꼈다. 마치 블로그에서 이것저것 기능 가져다 붙이는 것 처럼 처음 코딩하던 시절의 재미가 있었다. 이론 중심의 물리를 해온 입장에서 정답 없이 실험으로 확인해가는 실증적인 공학 방식이 낯설면서도 흥미로웠다.